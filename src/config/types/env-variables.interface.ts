export interface EnvVariablesInterface {
  printer: {
    front_printer_server_url: string;
    filter_printer_server_url: string;
    filter_printer_cups_url: string;
    filter_printer_name: string;
    front_printer_name: string;
  };
  service: {
    upload_bin_url: string;
    franchise_id: string;
  };
}
